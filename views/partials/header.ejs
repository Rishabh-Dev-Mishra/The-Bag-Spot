<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== "undefined" ? title : "The Bag Spot" %></title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="font-['helvetica_now_display'] bg-gray-50">
    <nav class="w-full flex justify-between px-5 py-3 bg-white shadow-md">
        <h3 class="text-xl font-bold text-gray-800">The Bag Spot</h3>
        <% 
            // Set loggedin to true by default if not explicitly passed
            var loggedin = typeof loggedin !== "undefined" ? loggedin : true; 
        %>
        <% if (loggedin) { %>
            <div class="flex gap-5 text-gray-600">
                <a class="hover:text-blue-500" href="/shop">Shop</a>
                <a class="hover:text-blue-500" href="/cart">Cart</a>
                <a class="hover:text-blue-500" href="#">My account</a>
                <a class="text-red-600 hover:text-red-800" href="/users/logout">Logout</a>
            </div>
        <% } else { %>
            <div class="flex gap-5 text-gray-600">
                <a class="hover:text-blue-500" href="/users/login">Login</a>
                <a class="hover:text-blue-500" href="/users/register">Register</a>
            </div>
        <% } %>
    </nav>
